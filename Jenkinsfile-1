pipeline {
  agent {
    
   docker login -u dockerkumars12 -p ARUNKUMAR123' 
    docker { image 'liquibase/liquibase' }
  }
  stages {
    stage('Test') {
      steps {
        sh 'liquibase --version'
      }
    }
  }
}
